const mission = process.argv[2];

mission === "learn"
  ? console.log("Time to write some node!")
  : console.log(`Is ${mission} really more fun?`);

// provide "explore" as input
// provide "learn" as input
